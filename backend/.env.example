# 后端系统环境变量配置模板
# 复制此文件为 .env 并填入实际值

# ==================== 服务器配置 ====================
# 服务器端口（默认: 8080）
PORT=8080

# ==================== CORS 配置 ====================
# 允许的跨域源，多个用逗号分隔（默认: * 允许所有）
# 生产环境建议设置为具体的前端域名，例如: http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=*

# ==================== 日志配置 ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR (默认: INFO)
LOG_LEVEL=INFO

# 日志保留天数（默认: 10）
LOG_RETENTION_DAYS=10

# 错误日志文件大小限制（默认: 50MB）
LOG_ERROR_MAX_SIZE=50 MB

# ==================== API 配置 ====================
# Yahoo Finance API 重试次数（默认: 3）
YFINANCE_MAX_RETRIES=3

# 数据缓存时间（秒，默认: 900，即15分钟）
DATA_CACHE_TTL=900

# ==================== 环境标识 ====================
# 运行环境: development, production, testing
ENVIRONMENT=development
